<x-app-layout>
    <x-slot name="header">
        <h2 class="font-semibold text-xl text-gray-800 leading-tight">
            Networks
        </h2>
    </x-slot>

    @php
        $q         = $filters['q'] ?? '';
        $countryId = $filters['country_id'] ?? null;
        $mccmnc    = $filters['mccmnc'] ?? '';
        $perPage   = $filters['per_page'] ?? 20;
    @endphp

    <div class="py-6 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 space-y-4">
        @includeIf('partials.flash_log')

        {{-- Filters + actions --}}
        <div class="bg-white p-4 rounded-lg shadow flex flex-col gap-4">
            <form method="GET" action="{{ route('networks.index') }}" class="grid grid-cols-1 md:grid-cols-4 gap-4 items-end">
                <div>
                    <label for="filter_q" class="block text-sm font-medium text-gray-700">
                        Name
                    </label>
                    <input
                        type="text"
                        id="filter_q"
                        name="q"
                        value="{{ $q }}"
                        class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm"
                        placeholder="Search by network name"
                    >
                </div>

                <div>
                    <label for="filter_country_id" class="block text-sm font-medium text-gray-700">
                        Country
                    </label>
                    <select
                        id="filter_country_id"
                        name="country_id"
                        class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm"
                    >
                        <option value="">All countries</option>
                        @foreach ($countries as $country)
                            <option value="{{ $country->id }}" @selected((int) $countryId === (int) $country->id)>
                                {{ $country->name }}
                            </option>
                        @endforeach
                    </select>
                </div>

                <div>
                    <label for="filter_mccmnc" class="block text-sm font-medium text-gray-700">
                        MCC / MNC
                    </label>
                    <input
                        type="text"
                        id="filter_mccmnc"
                        name="mccmnc"
                        value="{{ $mccmnc }}"
                        class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm"
                        placeholder="20201, 710, 01, ..."
                    >
                </div>

                <div class="flex flex-col md:flex-row gap-2 justify-end">
                    <div class="flex gap-2 justify-end">
                        <button
                            type="submit"
                            class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md shadow-sm bg-indigo-600 text-white hover:bg-indigo-700"
                        >
                            Apply
                        </button>
                        <a
                            href="{{ route('networks.index') }}"
                            class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md shadow-sm bg-white text-gray-700 hover:bg-gray-50"
                        >
                            Reset
                        </a>
                    </div>

                    <div class="flex gap-2 justify-end">
                        <a
                            href="{{ route('networks.index', array_merge(request()->query(), ['export' => 'csv'])) }}"
                            class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md shadow-sm bg-white text-gray-700 hover:bg-gray-50"
                        >
                            Export CSV
                        </a>
                        <a
                            href="{{ route('networks.create') }}"
                            class="inline-flex items-center px-4 py-2 border border-green-300 text-sm font-medium rounded-md shadow-sm bg-green-50 text-green-800 hover:bg-green-100"
                        >
                            Create Network
                        </a>
                    </div>
                </div>
            </form>
        </div>

        {{-- List --}}
        <div class="bg-white shadow overflow-hidden sm:rounded-lg">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Name</th>
                        <th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Country</th>
                        <th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">MCCs</th>
                        <th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">MNCs</th>
                        <th scope="col" class="px-4 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Notes</th>
                        <th scope="col" class="px-4 py-3 text-right text-xs font-medium text-gray-500 uppercase tracking-wider">Actions</th>
                    </tr>
                </thead>
                <tbody class="bg-white divide-y divide-gray-200">
                    @forelse ($networks as $network)
                        @php
                            $mccs = $mccsMap[$network->id] ?? '';
                            $mncs = $mncsMap[$network->id] ?? '';
                            $mccArray = $mccs !== '' ? explode(',', $mccs) : [];
                            $mncArray = $mncs !== '' ? explode(',', $mncs) : [];
                        @endphp
                        <tr>
                            <td class="px-4 py-3 text-sm text-gray-900">
                                {{ $network->name }}
                            </td>
                            <td class="px-4 py-3 text-sm text-gray-900">
                                {{ optional($network->country)->name ?? '—' }}
                            </td>
                            <td class="px-4 py-3 text-sm text-gray-900">
                                @if (count($mccArray))
                                    <div class="flex flex-wrap gap-1">
                                        @foreach ($mccArray as $mcc)
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-blue-50 text-blue-800">
                                                {{ $mcc }}
                                            </span>
                                        @endforeach
                                    </div>
                                @else
                                    <span class="text-xs text-gray-400">—</span>
                                @endif
                            </td>
                            <td class="px-4 py-3 text-sm text-gray-900">
                                @if (count($mncArray))
                                    <div class="flex flex-wrap gap-1">
                                        @foreach ($mncArray as $mnc)
                                            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-purple-50 text-purple-800">
                                                {{ $mnc }}
                                            </span>
                                        @endforeach
                                    </div>
                                @else
                                    <span class="text-xs text-gray-400">—</span>
                                @endif
                            </td>
                            <td class="px-4 py-3 text-sm text-gray-900">
                                @if ($network->meta && $network->meta->notes)
                                    <span class="line-clamp-2 text-xs text-gray-700">{{ $network->meta->notes }}</span>
                                @else
                                    <span class="text-xs text-gray-400">—</span>
                                @endif
                            </td>
                            <td class="px-4 py-3 text-sm text-right">
                                <a
                                    href="{{ route('networks.edit', $network->id) }}"
                                    class="inline-flex items-center px-3 py-1.5 border border-gray-300 text-xs font-medium rounded-md shadow-sm bg-white text-gray-700 hover:bg-gray-50"
                                >
                                    Edit
                                </a>
                            </td>
                        </tr>
                    @empty
                        <tr>
                            <td colspan="6" class="px-4 py-6 text-center text-sm text-gray-500">
                                No networks found.
                            </td>
                        </tr>
                    @endforelse
                </tbody>
            </table>

            {{-- Pagination + per-page selector --}}
            <div class="px-4 py-3 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3 border-t border-gray-200 bg-white">
                <div class="text-sm text-gray-700">
                    Showing
                    <span class="font-medium">{{ $networks->firstItem() }}</span>
                    to
                    <span class="font-medium">{{ $networks->lastItem() }}</span>
                    of
                    <span class="font-medium">{{ $networks->total() }}</span>
                    results
                </div>
                <div class="flex items-center gap-4">
                    <form method="GET" action="{{ route('networks.index') }}" class="flex items-center gap-2 text-sm">
                        <label for="per_page" class="text-gray-700">Per page</label>
                        <select
                            id="per_page"
                            name="per_page"
                            class="mt-1 block rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500 text-sm"
                            onchange="this.form.submit()"
                        >
                            @foreach ([10, 25, 50, 100, 200] as $size)
                                <option value="{{ $size }}" @selected((int) $perPage === (int) $size)>{{ $size }}</option>
                            @endforeach
                        </select>
                        {{-- keep current filters when changing per_page --}}
                        <input type="hidden" name="q" value="{{ $q }}">
                        <input type="hidden" name="country_id" value="{{ $countryId }}">
                        <input type="hidden" name="mccmnc" value="{{ $mccmnc }}">
                    </form>
                    <div>
                        {{ $networks->onEachSide(1)->links() }}
                    </div>
                </div>
            </div>
        </div>
    </div>
</x-app-layout>
